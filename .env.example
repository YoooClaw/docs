# Cloudflare Pages 部署所需环境变量
# 复制为 .env 后填入实际值（.env 已被 .gitignore 忽略，不要提交）

# Cloudflare 账户 ID（deploy 脚本通过 bun 自动加载 .env 注入 wrangler）
CLOUDFLARE_ACCOUNT_ID=

# Cloudflare API Token（本地登录可用 `wrangler login` 代替；CI 中必填）
# CLOUDFLARE_API_TOKEN=
